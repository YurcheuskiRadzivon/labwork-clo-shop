 Быстрый запуск:

  # 1. Создайте БД
  psql -U postgres -c "CREATE DATABASE makki_shop"
  psql -U postgres -d makki_shop -f database.sql

  # 2. Настройте config.php (параметры подключения к PostgreSQL)

  # 3. Запустите сервер
  cd G:\gitPr\site2
  php -S localhost:8000

  # 4. Откройте http://localhost:8000

  Реализованный функционал:

  Ес че бд вручную можно сделать если ты не оздоровительная группа
  
